# Specs

This directory contains implementation and experiment specs.

Use these specs to define what "done" means before changing code, evaluation
logic, paper artifacts, or workflow tooling.

Read first:

1. `../spec_driven_development.md`
2. `template.md`
3. the active specs in the table below

## Status Values

- `draft`: useful planning text, not ready for implementation.
- `draft active`: active workstream, but blocked by open questions or missing
  verification details.
- `locked / implementation-ready`: enough contract, acceptance criteria, and
  commands exist to start coding.
- `implemented`: accepted and verified.
- `superseded`: retained only for history, with a pointer to the replacement.

## Active Specs

| Spec | Status | Blocked By | Next Action |
| --- | --- | --- | --- |
| `0001-translatable-normal-vae-baseline.md` | draft active / reopened; scaffold is `scaffold_schema_ready`; model count is `topology_count_ready`; local data/metrics is `data_metrics_ready`; local selector/dataloader is `fixed_selectors_dataloader_ready`; local CPU dataloader pre-test is `local_benchmark_pretest_ready`; model/loss train-step is `model_loss_train_step_ready`; local HED/stain corruption QA is `corruption_ready`; kaggle smoke is `kaggle_smoke_ready` with local embedded packaging proof; synthetic no-dataset setup smoke is `kaggle_setup_smoke_ready` and has one remote `smoke_pass`; synthetic binary timing pretest contract is `kaggle_synthetic_timing_contract_ready` with local kernel/guard implementation and four remote `synthetic_timing_pass` runs; capped real-data runtime pretest contract is `real_data_runtime_pretest_contract_ready` with a local non-promotable runner/kernel/guard, upload-simulation proof, and identity/hash/CRC/window plus clean-validation loader proof lane; remote execution of the new lane and later eligibility evidence remain pending | Reopened on 2026-06-11 after correcting the latent target to `32x32x16`, replacing MSE with `L1 + 0.1 * (1 - SSIM) + beta * KL`, choosing stain-aware corruption, removing the final `tanh`, clarifying the Conv2d baseline, locking the fixed binomial low-pass stage transition, recording the Conv2d count target, verifying Kaggle T4 metadata as `machine_shape = "NvidiaTeslaT4"`, and tightening runtime, dataloader, paired-check, gate-health, selected-runtime debug, and tiny-overfit requirements. Narrow local slices implemented on 2026-06-12 cover topology count, data/metrics, selector/dataloader, local dataloader pre-test, and model/loss train-step contracts. The 2026-06-13 HED/stain corruption local slice now implements scikit-image-compatible PyTorch HED semantics, and the capped Kaggle smoke now verifies metadata-carrying UBC-format batches, real corruptor plumbing, local synthetic train steps with at least one applied corruption, and one clean-validation batch without becoming runtime selection evidence. The first remote real-data smoke version finished as `ERROR` with `ModuleNotFoundError: No module named 'eqvae'` because the sibling payload was not uploaded, so it produced no benchmark evidence. The 2026-06-17 setup-smoke slice now generates a single-file embedded no-dataset kernel and upload-simulation test for Kaggle packaging/API/import/artifact plumbing only; remote version 1 completed with non-promotable setup `smoke_pass`. The 2026-06-18 synthetic timing implementation adds a separate no-dataset GPU kernel, a streaming generated 2 GiB-scale UBC-format binary profile, both single-T4 and dual-T4/DDP attempts, projected epoch-time comparison by feasible global batch, separate non-promotable synthetic artifacts, and no `selected_runtime.json`; adversarial findings around artifact count, status consistency, DDP rank device scope, temporary rank files, `/kaggle/working` confinement, blocked claims, recommendation ordering, profile scale, payload profile guards, loader/H2D-only interpretation, DDP rank proof, `drop_last = false` projection fields, partial-run status, repeat-gate state, embedded-template freshness, and shortlist provenance were fixed locally. Remote v1 used the earlier 0.81 GB profile; remote v2 first used the current 2 GiB-scale profile; remote v3 is the corrected 2 GiB broad-screen evidence with 16/16 rows passing, zero fit-probe rows, per-rank DDP assignment proof, exact `effective_samples_per_epoch = 300000`, and both single and dual T4 modes; remote v4 is the current 5-warmup/25-measured repeated-shortlist evidence with 4/4 rows passing and repeat policy completed. Downloaded ignored v4 benchmark artifacts live under `runs/kaggle/synthetic_timing_repeat_2gib_v4`. The capped real-data runtime pretest contract now records 8,192 train / 2,048 validation fixed spread windows, synthetic-v4 parent rows plus sentinel rows, FP32-first compile/corruption staging, AMP carry-forward only from stable FP32 candidates, `compile_settle_steps = 5`, Dynamo counter-source requirements, non-promotable blocked claims, and no `selected_runtime.json` for the capped pretest. The real-data pretest runner/kernel now has local embedded packaging, upload-simulation import proof, guard checks for exact T4 metadata and the exact patch dataset source, local wrong-accelerator/non-promotion artifact coverage, file-hash/CRC/row-count/WSI/holdout/window proof fields, and a clean validation loader proof that deliberately does not claim corruption-RNG instrumentation. Tiny local fixtures can only produce `local_pass`; canonical real `pass` requires exact dataset slug, 300000/30000 rows, 322/39 WSIs, zero train/validation and masked-holdout overlap, and the locked 8,192/2,048 spread windows. Timed rows stay ineligible until compile/DDP/real dataloader-throughput/numerical/corruption/gate-health evidence is implemented and passes. Any Kaggle source attachment requires `KAGGLE_FULL_DATASET_CONFIRMED=1`, and the real-data smoke/pretest guards allow only the known patch dataset source. It is not safe for broad coding yet: fixed real 25-patch visual QA, selected-runtime debug, real train/resume/evaluator/artifact writers, future `SO(2)` count ceiling, Kaggle runtime two-T4 proof on real data, real fixed selector generation from real Kaggle shards, and final adversarial spec review remain blockers. Final paper claims remain blocked until the sealed masked-WSI test shard is generated and locked. | Complete the remaining linked real-data pretest evidence next: compile-settle/Dynamo accounting, DDP launch proof, real dataloader throughput matrix, paired numerical checks, corruption strategy compile-stability checks, and gate-health rows. Run the capped real-data pretest remotely only when intentionally ready to attach the 60GB dataset, with `KAGGLE_PUSH_CONFIRMED=1 KAGGLE_FULL_DATASET_CONFIRMED=1`. Do not treat local `schema_pass`, `local_pass`, setup `smoke_pass`, synthetic timing output, capped real-data pretest recommendations, failed remote v1 output, or hardened real-data `smoke_pass` outputs as Kaggle runtime selection. |
| `0002-strict-python-quality-gate.md` | active gate installed; production scope excludes historical `src/nn`; passing | Empty `main.py` was deleted and `pytorch-msssim` was removed from `pyproject.toml`/`uv.lock` on 2026-06-12. Historical exploratory `src/nn` remains as reference material by user decision, excluded from Ruff/BasedPyright production scopes, and forbidden as an import source for `src/eqvae`. The latest `./scripts/python_quality.sh` passed for production Python with 75 tests and 0 BasedPyright errors. Local `.venv` must exist before no-sync checks. | Keep new work in typed `src/eqvae`; do not import `src.nn`; keep `./scripts/python_quality.sh` passing before benchmark CLIs are implementation-ready. |
| `0003-kaggle-cli-execution-workflow.md` | draft active workflow scaffold; synthetic setup-smoke remote v1 passed; real-data smoke has local embedded packaging proof; synthetic timing remote v4 passed; real-data runtime pretest has local runner/kernel/guard proof | The setup-smoke kernel now launches a generated embedded no-dataset setup path and remote v1 completed with non-promotable `smoke_pass`. The real-data capped smoke now has embedded packaging and upload-simulation import proof, but any metadata with nonempty `dataset_sources`, `competition_sources`, `kernel_sources`, or `model_sources` is guarded by `KAGGLE_FULL_DATASET_CONFIRMED=1`, and the real-data smoke guard allows only the known patch dataset source. The synthetic timing workflow is now a separate no-dataset GPU kernel with empty source lists, fresh embedded payload plus template verification, local upload-simulation coverage, non-promotable artifacts, adversarial-review fixes, completed remote v1/v2/v3 broad-screen evidence, and completed remote v4 repeated-shortlist evidence; v4 uses the current 2 GiB-scale profile and marks the 5-warmup/25-measured repeat gate completed without selecting a runtime. The real-data runtime pretest workflow now has a local embedded kernel, exact source/T4 guard, upload-simulation import test, and selected-runtime rejection, but no remote pretest run or selected-runtime evidence. Full benchmark/full-run launchers are still not implemented. | Complete the missing linked real-data pretest evidence before treating any timed row as eligible; remote writes still require explicit user permission and `KAGGLE_PUSH_CONFIRMED=1`, and source-attachment writes also require `KAGGLE_FULL_DATASET_CONFIRMED=1`. |

Keep specs current. If implementation changes the contract, update the spec in
the same workstream.
